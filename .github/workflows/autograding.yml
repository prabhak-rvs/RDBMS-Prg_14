on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run PL/SQL test
        run: |
          echo "Install or configure an Oracle-compatible SQL tool before running test.sql."
          echo "The required test command is: @test.sql"
